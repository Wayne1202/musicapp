DATABASE_URL="postgresql://musicapp:musicapp@localhost:5432/musicapp?schema=public"
PORT=4000
CLIENT_ORIGIN="http://localhost:3000"

# Optional: enables real video titles/thumbnails/durations via the YouTube Data API.
# Without it, the server falls back to YouTube's public oEmbed endpoint (title + thumbnail,
# no duration).
YOUTUBE_API_KEY=
